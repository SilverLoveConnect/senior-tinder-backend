# 스토어 심사 준비 상태 점검 (백엔드 기준)

- 작성일: 2026-09-03
- 대상 저장소: `SilverLoveConnect/senior-tinder-backend` (브랜치 `claude/review-progress-check-159zhd`, `main`과 동일 커밋 `e59380b`)
- 점검 방법: `app/` 전체 소스 정독 + `docs/legal/*` + `alembic/versions/*` + 커밋 이력 대조
- 기준: Apple App Store Review Guidelines(1.2 UGC / 3.1.1 IAP / 5.1.1 개인정보 / 5.1.1(v) 계정삭제), Google Play(사용자 생성 콘텐츠·데이팅 앱 정책, 계정 삭제 요건, 데이터 안전 섹션)

---

## 0. 한 줄 결론

**"심사에 필요한 기능은 대체로 다 있는데, 심사가 실제로 묻는 '운영 증빙'과 '운영 도구'가 비어 있다."**
반려 위험은 기능 부재보다 **① 신고 처리 창구 부재 ② Google Play 계정삭제 웹 URL 부재 ③ 심사관이 실제로 앱을 정상 상태로 볼 수 있는가(사진 승인 파이프라인)** 세 곳에 몰려 있다.
백엔드 코드 완성도만 보면 6~7할이 아니라 **8할 이상**이다. 남은 2할이 전부 "심사 서류·운영" 쪽이라는 게 문제.

**단, "오늘 그대로 제출하면 통과하냐"는 질문에는 아니오다.** 최소 3건(계정삭제 URL / 신고 처리 창구 / 매칭 화면에 사진 0장)은 제출 전에 반드시 메워야 한다. 반대로 이 3건은 합쳐서 하루 안에 끝나는 분량이다.

---

## 1. 심사 항목별 판정표

| # | 심사 요구 | 상태 | 근거 |
|---|---|---|---|
| 1 | 회원 차단 기능 (Apple 1.2) | ✅ 충족 | `app/routers/block.py`, `app/services/block.py` |
| 2 | 차단의 **실효성** (피드/좋아요/매칭목록/채팅 전부 차단 반영) | ✅ 충족 | `app/services/matching.py:14`(양방향 `blocked_user_ids`), `matching.py:151`(좋아요 차단), `app/routers/chat.py:47`(채팅 차단), `matching.py:222`(매칭목록 제외) |
| 3 | 신고 기능 (Apple 1.2) | ✅ 기능 존재 | `app/routers/report.py`, `app/services/report.py` |
| 4 | **신고 처리 창구 / 24시간 내 조치** | ❌ **없음** | 신고 목록 조회 API가 없음(`GET /internal/reports` 부재). 운영자가 신고를 볼 수단이 DB 직접 조회뿐 |
| 5 | 부적절 콘텐츠 자동 필터 | ⚠️ 조건부 | 사진: `app/routers/users.py:135` → AI 서버 콜백 `internal.py:14`. 채팅: `chat.py:80` 스캠 탐지. **단, AI 서버 미배포/타임아웃이면 그냥 통과** |
| 6 | 부적절 판정 후 조치 | ⚠️ 반쪽 | 사진은 `rejected` 처리(`internal.py:31`), **채팅 스캠은 `is_scam`만 기록하고 차단/경고 없음**(`chat.py:107`) |
| 7 | 위반자 퇴출(ban) | ✅ 충족 | 신고 3회 자동 정지 `report.py:52`, 수동 정지/해제 `POST /internal/users/{id}/ban` |
| 8 | 계정 삭제 (앱 내) | ✅ 충족 | `DELETE /users/me` → `users.py:delete_account` (소프트삭제 + phone/name 익명화 + 사진 삭제 + fcm_token 제거) |
| 9 | **계정 삭제 웹 URL (Google Play 필수)** | ❌ **없음** | `docs/legal/`에 privacy·terms만 존재. Play 콘솔 "계정 삭제 요청 URL" 입력란을 채울 페이지가 없음 |
| 10 | 개인정보 처리방침 게시 | ✅ 충족 | `docs/legal/privacy.html` (GitHub Pages), 수집항목·보유기간·AI 자동처리 고지까지 포함 |
| 11 | 이용약관 / EULA | ⚠️ 거의 충족 | `docs/legal/terms.html` 제6·7조에 금지행위·신고차단·제재 명시. **단 "부적절 콘텐츠 무관용(zero tolerance)" 문구와 "신고 24시간 내 처리" 약속 문구가 없음** — Apple 1.2가 명시적으로 요구하는 표현 |
| 12 | 문의 연락처 게시 | ✅ 충족 | 약관/방침 내 `e53788989@gmail.com` |
| 13 | 심사관용 데모 계정 | ✅ 구현됨 | `REVIEW_TEST_PHONE`/`REVIEW_TEST_CODE` (`app/services/auth.py:35`) — 둘 다 설정 시에만 활성, rate limit·실제 SMS 우회 |
| 14 | 디지털 재화 IAP 미준수 회피 (Apple 3.1.1) | ✅ 충족 | `PAYMENTS_ENABLED=False` 기본값 + `require_payments_enabled`로 서버에서도 404 (`app/routers/payment.py:21`) |
| 15 | 연령 제한 / 성인 확인 | ✅ 충족 | 가입 나이 50~100 강제(`schemas/auth.py:RegisterRequest`), SMS 본인확인 |
| 16 | 데이터 안전 섹션(Play) / 개인정보 라벨(Apple) 작성 | ❓ 미확인 | 저장소 밖 작업. 방침 문서와 실제 수집 항목은 일치하므로 작성 자체는 어렵지 않음 |
| 17 | 배포·운영 안정성 | ⚠️ | `/health` + Sentry 있음. **테스트 코드 0건, CI 없음**(`.github/`에 PR 템플릿만) |

---

## 2. 잘 되어 있는 것 (객관적으로 칭찬할 부분)

1. **차단이 "말뿐인 차단"이 아니다.**
   대부분의 팀이 여기서 반려된다 — 차단 버튼은 있는데 API를 직접 부르면 매칭도 되고 푸시도 간다.
   이 저장소는 `e59380b` 커밋에서 피드·좋아요·매칭목록·채팅 4개 경로 전부에 양방향 차단을 넣었다. 심사관이 두 계정으로 실제 테스트해도 뚫리지 않는다.

2. **결제를 서버에서까지 닫았다.**
   프론트에서 진입점만 숨기고 서버는 열어두는 게 흔한 실수인데(토큰만 있으면 호출 가능 → Apple 3.1.1 위반 증거가 됨), `require_payments_enabled`로 404를 반환한다. 403이 아니라 404를 고른 판단도 맞다.

3. **탈퇴가 형식적이지 않다.**
   `is_active=False`만 찍고 끝내는 경우가 많은데, phone을 `deleted:{id}`로 바꿔 재가입 가능하게 하고, 이름/닉네임 익명화 + 사진 전부 삭제 + FCM 토큰 제거까지 한다.

4. **심사관 로그인 우회 경로를 "둘 다 설정됐을 때만" 켜지게 방어했다.**
   `REVIEW_TEST_CODE`가 비었을 때 빈 코드 레코드가 생겨 아무도 로그인 못 하는 함정을 주석으로 남기고 막아뒀다.

5. **신뢰점수 중복 적립·수동검수 탈출구 등 리뷰 지적 11건을 실제로 반영했다.** (`e59380b`)

---

## 3. 반려로 이어질 수 있는 블로커 (우선순위 순)

### 🔴 B-1. 신고 처리 창구가 없다 — Apple 1.2 직격
- **현상**: `POST /reports`로 신고는 쌓이는데, 운영자가 **신고 목록을 볼 API가 없다.** `internal.py`에는 사진 검수(`/photos/pending`, `/photos/{id}/review`)와 ban만 있고 report는 없다.
- **왜 문제인가**: Apple 1.2는 "신고된 콘텐츠를 **24시간 내에** 검토하고 조치, 위반자 퇴출"을 요구한다. 심사 노트에 "신고 접수 후 어떤 프로세스로 처리하나요?"가 오면 답할 근거가 없다. 자동 3회 누적 정지만으로는 "검토(review)"가 아니다.
- **필요 작업**: `GET /internal/reports?status=pending`(목록) + `POST /internal/reports/{id}/resolve`(처리/기각) 2개. 기존 `verify_internal_token` 의존성 그대로 재사용 가능 — 새 레이어를 만들 필요 없음.
- **공수**: 반나절.

### 🔴 B-2. Google Play 계정 삭제 웹 URL 부재
- **현상**: 앱 내 탈퇴(`DELETE /users/me`)는 있으나, Play 콘솔이 요구하는 **앱을 설치하지 않고도 삭제를 요청할 수 있는 웹 페이지 URL**이 없다.
- **왜 문제인가**: 2023년 이후 Play는 이 URL이 없으면 데이터 안전 섹션 검토에서 반려한다. 실제 반려 사유 상위권.
- **필요 작업**: `docs/legal/account-deletion.html` 1장. "① 앱 내 내 정보 > 회원 탈퇴 ② 또는 e53788989@gmail.com으로 가입 전화번호와 함께 삭제 요청 ③ 삭제되는 데이터/보관되는 데이터(법정 보존분) 명시". 이미 GitHub Pages가 붙어 있으므로 파일만 추가하면 URL이 생긴다.
- **공수**: 1시간.

### 🟠 B-3. 심사관이 "빈 앱"을 볼 위험 (사진 승인 파이프라인)
- **현상**: 사진 업로드는 항상 `is_approved=False`로 저장(`users.py:131`)되고, 승인은 **AI 서버 콜백**(`POST /internal/ai/photo-result`) 또는 **관리자 수동 검수 API**로만 이뤄진다. AI 호출은 `timeout=3`에 `except: pass`(`users.py:139`)라 실패해도 조용히 넘어간다.
- **왜 문제인가**: AI 서버가 안 떠 있으면 **업로드한 사진이 영원히 승인되지 않는다.** 프로필 조회(`users.py:get_profile`)와 매칭 피드는 `is_approved` 사진만 노출하므로, 심사관 눈에는 "사진이 안 올라가는 앱 / 프로필 사진이 하나도 없는 매칭 앱"으로 보인다. Guideline 2.1(불완전한 앱)로 반려되는 전형적 형태.
- **참고**: 과거 커밋 `501a37d`("사진 업로드 즉시 승인 — AI 서버 배포 전 임시")로 한 번 우회했던 이력이 있는데, 현재 코드에는 그 우회가 남아 있지 않다.
- **함께 확인된 사실**: `scripts/seed.py`는 User/UserProfile/Point만 만들고 **`UserPhoto`를 단 한 건도 생성하지 않는다**(전체 105줄에 photo 관련 코드 0). 즉 시드 더미 20명은 전부 `photos: []`다. 심사관이 매칭 화면에 들어가면 **사진 없는 카드 20장**을 보게 된다 — 데이팅 앱에서 이건 Guideline 2.1(완성되지 않은 앱)로 직행하는 그림이다. **이 항목만 놓고 보면 현재 상태로 제출하면 반려 가능성이 높다.**
- **필요 작업(택1)**: ① 심사 기간 중 AI 서버 가동을 보장하고 실제로 콜백이 오는지 검증 ② 콜백 미도착 사진을 N분 후 pending으로 올려 수동 승인 ③ `scripts/seed.py`에 `is_approved=True`인 `UserPhoto`를 추가해 시드 프로필이 사진을 갖게 하기(가장 빠른 조치).
- **공수**: 검증 반나절 / 폴백 구현 반나절.

### 🟠 B-4. 약관에 Apple이 찾는 문구가 없다
- 제6·7조에 금지행위와 제재는 있으나, **"부적절한 콘텐츠 및 악성 이용자에 대한 무관용 정책"**, **"신고 접수 시 24시간 이내 검토·조치"** 라는 명시 문장이 없다. 심사관은 이 문구를 키워드로 찾는다.
- **공수**: 30분 (약관 제7조에 2개 항 추가).

---

## 4. 심사보다 "사고"가 무서운 리스크

### 🔴 R-1. `INTERNAL_TOKEN` 미설정 시 내부 API가 전부 무인증 공개
```python
# app/core/dependencies.py:78
if not settings.INTERNAL_TOKEN:
    # 환경변수 미설정 시 개발 환경으로 간주, 통과
    return
```
- 환경변수를 한 번 빠뜨리면 `/internal/photos/pending`(전체 사용자 사진 S3 URL + user_id 노출), `/internal/users/{id}/ban`(**임의 계정 정지**), `/internal/photos/{id}/review`(임의 승인)가 인터넷에 열린다.
- **조치**: 프로덕션에서는 미설정 시 기동 실패(`Field(min_length=1)`)로 바꾸거나, 최소한 `DEBUG` 플래그가 켜진 경우에만 통과시킬 것. `DATABASE_URL`·`SECRET_KEY`는 이미 이 방식으로 강제하고 있으므로 **기존 관례를 그대로 따르면 된다.**

### 🟠 R-2. 전화번호 정규화가 일관되지 않다
- `_normalize_phone`은 심사 번호 **비교**에만 쓰이고, 저장(`User.phone`, `SmsVerification.phone`)은 요청 원문 그대로다. 스키마가 `^010[0-9]{8}$`로 강제하니 앱 경유는 안전하지만, 심사용 번호를 `.env`에 하이픈 포함으로 넣고 DB에는 하이픈 없이 계정을 만들어 두면 **SMS 발송 우회는 되는데 로그인에서 "존재하지 않는 회원"이 난다.** 심사 직전에 반드시 실제 로그인까지 리허설할 것.

### 🟠 R-3. 채팅 스캠 탐지가 탐지만 하고 아무것도 안 한다
- `is_scam=True`여도 메시지는 그대로 저장·전송되고 상대 폰에 푸시까지 간다(`chat.py:107~121`). 앱이 "AI 사기 탐지"를 셀링포인트로 쓴다면 심사관이 실제 동작을 물었을 때 보여줄 게 없다. 최소한 응답의 `is_scam`을 프론트가 경고 배너로 쓰는지 확인 필요.

### 🟡 R-4. 읽음 처리 API가 없다
- `ChatMessage.is_read`는 어디서도 `True`로 갱신되지 않는다(`grep is_read` 결과: 조회 3곳, 갱신 0곳). `unread_count`가 영원히 누적된다. 심사 반려 사유는 아니지만 심사관 눈에 "안 읽은 메시지 47" 같은 화면이 걸리면 완성도 의심을 산다.

### 🟡 R-5. 테스트 0건 / CI 없음
- `pyproject.toml`의 `dev` 의존성 그룹이 비어 있고 `tests/` 디렉터리가 없다. 심사와 무관하지만, 지금 속도로 수정이 들어가면 **차단 실효성 같은 이미 고친 항목이 조용히 깨질 수 있다.** 최소한 차단·탈퇴·결제차단 3개는 회귀 테스트를 붙이는 게 좋다.

---

## 5. 남은 작업 우선순위 (심사 제출 기준)

| 순위 | 작업 | 성격 | 예상 공수 |
|---|---|---|---|
| 1 | `docs/legal/account-deletion.html` 추가 (Play 필수 URL) | 문서 | 1h |
| 2 | 약관에 무관용·24시간 조치 문구 추가 | 문서 | 30m |
| 3 | 신고 목록/처리 내부 API 2개 (`GET /internal/reports`, `POST /internal/reports/{id}/resolve`) | 코드 | 4h |
| 4 | `INTERNAL_TOKEN` 미설정 시 통과 제거 | 코드(보안) | 30m |
| 5 | 시드 더미에 승인된 사진 추가(`scripts/seed.py`) — 빈 매칭 화면 방지 | 코드 | 2h |
| 6 | 사진 승인 파이프라인 실환경 검증 + 폴백 | 코드/운영 | 4h |
| 7 | 심사관 계정 end-to-end 리허설(발송→검증→가입→로그인→매칭→채팅→차단→신고→탈퇴) | 검증 | 2h |
| 8 | 채팅 읽음 처리 API | 코드 | 2h |
| 9 | 회귀 테스트 3종 + CI | 코드 | 1d |

**1~4번(약 6시간)만 끝내면 "심사 서류상 결격 사유는 없는 상태"가 된다.** 5~7번은 "심사관이 앱을 제대로 못 봐서 나는 반려"를 막는 작업이라 실질 중요도는 오히려 더 높다 — 특히 5번(빈 사진)은 지금 제출하면 바로 걸린다.

---

## 6. 심사 제출 직전 체크리스트 (환경변수)

프로덕션(Railway)에서 아래가 **모두** 채워져 있는지 확인:

| 변수 | 미설정 시 증상 |
|---|---|
| `INTERNAL_TOKEN` | 내부 API 전면 무인증 공개 (R-1) |
| `CORS_ALLOWED_ORIGINS` | `*` + `allow_credentials=True` 조합 (모바일 앱은 무해하나 권장 안 함) |
| `REVIEW_TEST_PHONE` / `REVIEW_TEST_CODE` | 심사관이 SMS를 못 받아 로그인 불가 → 확정 반려 |
| `SOLAPI_*` | 일반 사용자 인증번호 미발송(예외를 삼켜서 조용히 실패, `sms.py:26`) |
| `AWS_S3_*` | 사진 업로드 500 |
| `AI_IMAGE_API_URL` / `AI_API_URL` | 사진 영구 미승인(B-3), 스캠 탐지 무동작 |
| `FIREBASE_CREDENTIALS_JSON` | 푸시 전무 |
| `PAYMENTS_ENABLED` | **반드시 `false` 유지** (true면 Apple 3.1.1 반려) |
| `SENTRY_DSN` | 심사 중 오류 추적 불가 |

---

## 7. 결론 요약

- **기능 구현은 심사 통과선을 넘었다.** 차단·신고·탈퇴·결제차단·심사관 계정 등 Apple 1.2/3.1.1/5.1.1 대응이 코드로 실재하고, 특히 차단 실효성은 평균 이상이다.
- **부족한 것은 코드가 아니라 "심사가 요구하는 운영의 증거"다.** 신고를 사람이 검토하는 창구, Play용 계정삭제 URL, 약관 문구 — 합쳐서 하루면 끝나는 분량인데 없으면 그대로 반려된다.
- **가장 조용하고 위험한 항목은 B-3(사진 승인)과 R-1(INTERNAL_TOKEN)이다.** 둘 다 "설정만 되어 있으면 잘 돌지만, 안 되어 있으면 아무도 모르게 실패"하는 구조다. 심사 전 실환경 리허설이 반드시 필요하다.
